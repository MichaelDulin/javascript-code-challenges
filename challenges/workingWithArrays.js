"use strict";


/*--------------------------------------------------------------------
----------------------------- Even Array -----------------------------
    Given an array, return a new array with only even numbers:

    IN: [1, 2, 3, 4, 5, 6, 7, 8]
    OUT: [2, 4, 6, 8]
*/

const evenArray = (arr) => {};





/*  ----------------------------------------------------------------------
----------------------------- Odd Array ----------------------------------
    Given an array, return a new array with only odd numbers:

    IN: [1, 2, 3, 4, 5, 6, 7, 8]
    OUT: [1, 3, 5, 7]
*/

const oddArray = (arr) => {};





/*  ----------------------------------------------------------------------
----------------------------- Find Values --------------------------------
    Given an array and a value, return how many times the value was found:

    IN: [1, 2, 3, 4, 1, 6, 1, 8]
    OUT: 3
*/

const compareArray = (arr1, arr2) => {};





/*  -------------------------------------------------------------------------
----------------------------- Find Middle Value -----------------------------
    Given an array, return the number found in the middle of said array:

    IN: [1, 6, 2, 9, 10, 11, 4]
    OUT: 9
*/

const findMiddleVal = (arr) => {};





/*  ------------------------------------------------------------------------------
----------------------------- More Middle Index Work -----------------------------
    Given an array, find the middle index, then add the number to the left and right together and return:

    IN: [1, 6, 2, 9, 10, 11, 4]
    OUT: 12
*/

const addMiddleNeighbors = (arr) => {};





/*  ---------------------------------------------------------------------------------
----------------------------- Getting Weird With Arrays -----------------------------
    Alright. This one is a bit tricky. Given an array, find all even values and them them to a new array.
    Do the same for the odd values. THEN add each array together, index to index. 
    Return final array.

    EX: If given [1,2,3,4], then evenArray = [2, 4] and oddArray = [1, 3]. Then add -> [3, 7] -> return that

    IN: [1, 6, 2, 9, 10, 11, 4, 17, 3, 100, 1001, 12]
    OUT: [7, 11, 21, 103, 1013]

*/

const gettingWeird = (arr) => {};
















/* ============================================================================ */
/* ============================================================================ */
/* ============================================================================ */



module.exports = {
    evenArray,
    oddArray,
    compareArray,
    findMiddleVal,
    addMiddleNeighbors,
    gettingWeird
};
